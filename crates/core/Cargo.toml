[package]
name = "grit_core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
uniffi = "0.25"
thiserror = "1.0"

[build-dependencies]
uniffi = { version = "0.25", features = ["build"] }
