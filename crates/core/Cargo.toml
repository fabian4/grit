[package]
name = "grit_core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
uniffi = "0.31"
thiserror = "1.0"
uniffi_bindgen = "0.31"
camino = "1.1"

[build-dependencies]
uniffi = { version = "0.31", features = ["build"] }
